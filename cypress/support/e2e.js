// Import commands.js
import './commands';

// Preserve cookies between tests
Cypress.Cookies.defaults({
  preserve: ['session_id', 'auth_token'],
});// Import commands.js
import './commands';

// Preserve cookies between tests
Cypress.Cookies.defaults({
  preserve: ['session_id', 'auth_token'],
});